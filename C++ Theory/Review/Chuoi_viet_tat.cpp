#include <iostream>
#include <string>

using namespace std;

string fullChain(string str) {
    for(int i = 0; i < str.size(); i++) {
        int val;
        if(str[i] == '[') {
            val = str[i];

        }
    }
}

int main() {
    string str;
    getline(cin, str);

    cout << str;
    return 0;
}